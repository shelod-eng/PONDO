services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pondo
    ports:
      - "5432:5432"
    volumes:
      - pondo_pgdata:/var/lib/postgresql/data

volumes:
  pondo_pgdata: {}

